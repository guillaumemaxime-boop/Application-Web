# Shell scripts must use LF line endings or sh chokes on the \r characters
# (especially noticeable inside Linux containers built from a Windows checkout).
*.sh        text eol=lf
entrypoint  text eol=lf
Dockerfile  text eol=lf
